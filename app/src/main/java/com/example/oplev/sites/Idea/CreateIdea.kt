package com.example.oplev.sites.Idea

import androidx.compose.foundation.layout.*
import androidx.compose.material.Scaffold
import androidx.compose.material.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.tooling.preview.Preview
import androidx.compose.ui.unit.dp
import com.example.oplev.ViewModel.*
import com.example.oplev.sites.TopBar

/*
@Composable
fun CreateIdea(CreateIdeaViewModel: CreateIdeaViewModel, modifier: Modifier = Modifier) {
    Scaffold(
        topBar = { TopBar(title = "Velkommen {user}") },
        content = {
            Column(modifier = Modifier.fillMaxSize()) {
                Box(modifier = Modifier.height(120.dp)) {
                    Column(
                        modifier = Modifier
                            .fillMaxHeight()
                            .fillMaxWidth()
                    )
                    {
                        Text(text = "Create Idea")
                    }

                }
                )

            }
            })
}
*/




